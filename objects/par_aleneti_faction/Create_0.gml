/// @description 
event_inherited(); //do the parent's inherited create event (par_actor), then our create event

faction = "aleneti"; //what side they are affliliated with, affects turn order
ai_type = "reaction";